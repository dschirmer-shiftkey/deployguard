{
  "name": "@trailhead/mcp-server",
  "version": "4.1.0",
  "private": true,
  "description": "MCP server exposing Trailhead deployment gate — health checks, risk scoring, DORA metrics, and policy enforcement as tools",
  "type": "module",
  "main": "dist/server.js",
  "bin": {
    "trailhead-mcp": "dist/server.js"
  },
  "scripts": {
    "prebuild": "node -e \"const fs=require('fs');fs.copyFileSync('../src/risk-engine.ts','src/risk-engine.ts');fs.mkdirSync('src/adapters',{recursive:true});['types','registry','vercel','supabase','aws-ecs','fly-io','cloudflare','index'].forEach(f=>fs.copyFileSync('../src/adapters/'+f+'.ts','src/adapters/'+f+'.ts'))\"",
    "build": "tsc",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^4.4.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
