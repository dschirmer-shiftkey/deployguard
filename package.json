{
  "name": "deployguard",
  "version": "0.1.0",
  "private": true,
  "description": "DeployGuard — self-healing CI/CD deployment gates with MCP-based infrastructure health checks",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/main.ts",
    "lint": "tsc --noEmit",
    "test": "echo 'no tests configured yet'"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
